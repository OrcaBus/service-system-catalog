import * as path from 'path';

export const APP_ROOT = path.join(__dirname, '../../app');
export const REPO_ROOT = path.join(__dirname, '../..');
export const OPENAPI_SPEC_PATH = path.join(APP_ROOT, 'schema/openapi.yaml');
